spring:
  datasource:
    url: jdbc:mysql://localhost/trazar?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false
    username: root
    password: 

  jpa:
    hibernate:  
      ddl-auto: update
      
server:
  error:
    include-message: always